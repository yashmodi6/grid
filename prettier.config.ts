import { type Config } from "prettier";

const config: Config = {
  semi: true,
  tabWidth: 2,
  trailingComma: "es5",
  printWidth: 100,
  bracketSameLine: true,
  bracketSpacing: true,
  arrowParens: true,
};

export default config;
